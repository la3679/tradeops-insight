# ADR 0010: Public and synthetic data licensing

Status: accepted. All operational records are deterministic synthetic data. Small public fixtures are limited to reference shapes, recorded with source URL, terms URL, retrieval time, transformation, count, path, and hash. Fixture-only local mode prevents hidden network dependence; refreshed sources require review and a new manifest.
