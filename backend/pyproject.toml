[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tradeops-copilot-backend"
version = "0.1.0"
description = "Typed API and worker foundation for the TradeOps Copilot portfolio project"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
  "alembic==1.19.1",
  "celery==5.6.3",
  "faiss-cpu==1.15.0",
  "fastapi==0.141.1",
  "langgraph==1.2.11",
  "langgraph-checkpoint-postgres==3.1.2",
  "opentelemetry-exporter-otlp-proto-grpc==1.44.0",
  "opentelemetry-instrumentation-fastapi==0.65b0",
  "opentelemetry-sdk==1.44.0",
  "psycopg[binary,pool]==3.3.4",
  "PyJWT[crypto]==2.13.0",
  "prometheus-client==0.26.0",
  "pydantic-settings==2.15.0",
  "redis==8.1.0",
  "sqlalchemy==2.0.52",
  "structlog==26.1.0",
  "uvicorn==0.52.4",
]

[project.scripts]
tradeops-api = "tradeops.api.__main__:main"
tradeops-worker = "tradeops.worker.__main__:main"

[dependency-groups]
dev = ["httpx==0.28.1", "mypy==2.3.1", "pre-commit==4.6.2", "pytest==9.1.1", "pytest-cov==7.1.0", "ruff==0.16.4"]

[tool.hatch.build.targets.wheel]
packages = ["src/tradeops"]

[tool.pytest.ini_options]
addopts = "--strict-config --strict-markers --cov=tradeops --cov-report=term-missing --cov-fail-under=90"
testpaths = ["tests"]

[tool.coverage.run]
branch = true
omit = [
  "src/tradeops/**/__main__.py",
  # Declarative schema definitions contain no executable business behavior.
  "src/tradeops/adapters/persistence/models.py",
]

[tool.mypy]
python_version = "3.14"
strict = true
packages = ["tradeops"]
mypy_path = "src"

[[tool.mypy.overrides]]
module = "celery"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "faiss"
ignore_missing_imports = true

[tool.ruff]
target-version = "py314"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "RUF", "ASYNC"]
