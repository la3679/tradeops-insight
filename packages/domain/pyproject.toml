[project]
name = "tradeops-domain"
version = "0.1.0"
description = "Framework-independent TradeOps Copilot domain model and reconciliation rules."
requires-python = ">=3.12,<3.14"
license = "Apache-2.0"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tradeops_domain"]
