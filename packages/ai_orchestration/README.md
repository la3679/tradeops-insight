# TradeOps AI orchestration

This package owns typed advisory-provider and retrieval ports plus the governed workflow. Model output remains untrusted advisory data until schema, evidence, confidence, policy, authorization, and human-review gates pass.

The deterministic mock provider is the default and the only provider used by offline CI. No provider receives unrestricted tools, raw authorization material, real financial data, or authority to mutate state.
