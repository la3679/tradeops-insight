{
  "annotations": { "list": [] },
  "editable": false,
  "panels": [
    {
      "type": "timeseries",
      "title": "API request rate by status class",
      "targets": [
        {
          "expr": "sum by (status_class) (rate(tradeops_http_requests_total[5m]))",
          "legendFormat": "{{status_class}}"
        }
      ],
      "gridPos": { "h": 9, "w": 12, "x": 0, "y": 0 }
    },
    {
      "type": "timeseries",
      "title": "API p95 request duration",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum by (le) (rate(tradeops_http_request_duration_seconds_bucket[5m])))"
        }
      ],
      "gridPos": { "h": 9, "w": 12, "x": 12, "y": 0 }
    }
  ],
  "schemaVersion": 41,
  "tags": ["synthetic-demo"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "TradeOps Copilot — Local Demo",
  "uid": "tradeops-local",
  "version": 1
}
